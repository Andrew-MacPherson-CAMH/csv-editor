streamlit>=1.48
pandas>=2.0
PyYAML>=6.0
requests>=2.31            # gcloud_identity auth provider

# Optional — only needed when storage.provider = bigquery
# google-cloud-bigquery>=3.20
# db-dtypes>=1.2
